// Mirrors the --z-* custom properties in tokens.css. Use for inline styles
// on portaled elements (drag overlays, modals) where a CSS class isn't handy.
export const Z_INDEX = {
  base: 0,
  sticky: 10,
  dropdown: 20,
  header: 30,
  dragOverlay: 40,
  modalBackdrop: 50,
  modal: 51,
  popover: 60,
  toast: 70,
  tooltip: 80,
};
